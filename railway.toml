[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=/app/src uvicorn momentum_edge.api.app:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
